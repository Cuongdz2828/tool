def logic_top1(sorted_rooms, killed_room_id):
    return sorted_rooms[0][0], sorted_rooms[0][1]

def logic_top2(sorted_rooms, killed_room_id):
    return sorted_rooms[1][0], sorted_rooms[1][1]

def logic_random_top3(sorted_rooms, killed_room_id):
    choice = random.choice(sorted_rooms[:3])
    return choice[0], choice[1]

def logic_avoid_last(sorted_rooms, killed_room_id):
    for room_id, rate in sorted_rooms:
        if str(room_id) != str(killed_room_id):
            return room_id, rate
    return None, None

logic_funcs = {
    "logic1": logic_top1,
    "logic2": logic_top2,
    "logic3": logic_random_top3,
    "logic4": logic_avoid_last,
}
